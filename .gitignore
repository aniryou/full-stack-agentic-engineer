# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/

# OS / editor
.DS_Store
.idea/
.vscode/

# Secrets (keep the .example templates)
.env
!.env.example

# Terraform (never commit real variable files in a public repo; keep the .example templates)
.terraform/
terraform.tfvars
*.auto.tfvars
!terraform.tfvars.example
*.tfstate
*.tfstate.*

# Inbox: keep the folder's README, ignore dropped content until it's sorted into a layer
/raw/*
!/raw/README.md

# notebook runner scratch + terraform lockfiles from local validation
_run_outputs/
.terraform.lock.hcl

# guide site build output (mkdocs site_dir)
/site_build/
