# CLAUDE.md — how to work in `~/code/learning`

This is a **single mono-repo** that mirrors the LLM inference / serving stack, from physical GPUs
at the bottom to the agent application at the top. New material lands in `raw/` and gets filed into
the layer it belongs to. This file tells you (Claude) how to keep it consistent.

- **Incoming-content guidelines:** [`raw/README.md`](raw/README.md).
- **GitHub remote:** `https://github.com/aniryou/full-stack-agentic-engineer` (**public**), branch `main`.
- **Colab:** per-notebook "Open in Colab" links live in each layer's `README.md`; [`COLAB.md`](COLAB.md) holds the one-time setup. Both regenerated by `tools/gen_colab_index.py`.

> **Golden rule:** `raw/` is an *inbox*. Content is **moved** out of it into a numbered layer folder —
> never copied. `raw/` is gitignored except `raw/README.md`, so dropped content is not committed until
> it has been sorted into a layer.

---

## The stack (bottom-up: 01 = hardware, 07 = application)

```
  ┌─ 07-application-agent-framework   Application / agent framework
  │  06-gateway                       auth, rate limits, routing, quotas, observability, cost
  │  05-orchestrator                  Dynamo, llm-d, Ray Serve (routing, autoscaling, disaggregation)
  │  04-inference-engine              vLLM / SGLang / TensorRT-LLM
  │  03-kubernetes-gpu                Kubernetes + GPU Operator + scheduler
  │  02-cuda-nccl-runtime             container runtime, NCCL, CUDA, driver
  └─ 01-hardware-gpu-fabric           GPUs, NVLink, NICs, storage, cooling
```

| # | Folder | What belongs here | Signal keywords |
|---|--------|-------------------|-----------------|
| 07 | `07-application-agent-framework` | The agent: loop, tools, memory, durable execution, multi-agent, evals, RAG/retrieval, end-user apps | agent loop, tool calling, ADK, LangGraph, multi-agent, durable, long-running, checkpoint, saga, HITL, RAG, retrieval, vector store, eval |
| 06 | `06-gateway` | Control plane in front of models: authn/z, agent identity, policy/guardrails, rate limits, admission, routing, quotas, cost accounting, observability | auth, OAuth, OIDC, SPIFFE, token exchange, policy, guardrail, rate limit, admission, quota, cost/conversation, circuit breaker, audit, MCP authz |
| 05 | `05-orchestrator` | Coordinating engine replicas: routing, replica autoscaling, prefill/decode disaggregation, KV-aware routing | Dynamo, llm-d, Ray Serve, disaggregation, KV-aware routing, prefix cache, replica autoscaling, LoRA routing, SLO |
| 04 | `04-inference-engine` | Running a model on GPUs: KV cache, batching, prefill/decode, quantization, speculative decoding, attention kernels | vLLM, SGLang, TensorRT-LLM, KV cache, paged attention, flash attention, continuous batching, quantization, FP8, tensor parallel, TTFT |
| 03 | `03-kubernetes-gpu` | Cluster resource mgmt: GPU Operator, device plugins, GPU scheduling, gang/topology-aware placement | Kubernetes, GPU Operator, device plugin, gang scheduling, Volcano, Kueue, topology-aware, NUMA, cluster autoscaler |
| 02 | `02-cuda-nccl-runtime` | GPU software substrate: driver, CUDA, cuDNN, NCCL collectives, container runtime, MIG | CUDA, cuDNN, NCCL, all-reduce, collective, driver, MIG, nvidia-container-toolkit, containerd, kernel |
| 01 | `01-hardware-gpu-fabric` | Physical layer: GPU SKUs & memory, NVLink/NVSwitch, RDMA NICs, topology, storage, power/cooling, capacity | NVLink, NVSwitch, InfiniBand, RoCE, HBM, memory bandwidth, GPU capacity, rail-optimized, GPUDirect, cooling, TCO |
| 00 | `00-foundations` | Model-level material underpinning all layers: transformer internals, GPU capacity planning, model landscape | transformer, attention, embeddings, tiny GPT, capacity planning, TTFT/TPOT, open-weight, model landscape |

Each layer folder has a `README.md` with full scope + current contents. Plus **`00-foundations/`** (below the stack) for model-level material that underpins every layer. Layers 02, 03 and 05 are scaffolded
and (as of the last sort) empty.

## Within a layer: topic sub-folders

Items are grouped by topic; each source lab keeps its name and moves **as a whole unit** into a topic
sub-folder (e.g. `06-gateway/identity-security/`, `07-.../long-running-durable/`). Create a new topic
sub-folder for a new sub-domain; reuse an existing one when it fits. Kebab-case names.

## How to classify a new `raw/` item
1. Read enough to know what it is (README, primer/docs, module names) — not the folder name alone.
2. Pick **one** primary layer by the signal keywords — the layer whose *job* the content is about.
3. Choose/create a topic sub-folder in that layer.
4. **Cross-reference, don't split.** If it substantially covers a second layer, leave it whole and add
   a one-line note in both layer READMEs.
5. Retrieval/RAG currently lives under `07-.../retrieval-rag/`; graduate it to its own area only if a
   distinct data/retrieval-infra theme accumulates (ask first).

---

## Mono-repo & Colab

- **One git repo** at the root. No per-lab `.git`. Each lab keeps its **own** `requirements.txt` /
  `pyproject.toml`; there is no repo-wide environment.
- **Every notebook is Colab-ready.** Its first cell (tagged `colab-bootstrap`) is a no-op locally, and
  on Colab clones this repo (public, so a plain shallow clone), `cd`s into the notebook's dir, and pip-installs the nearest lab; see `COLAB.md`.
  Two mechanisms, both baking in the repo URL:
  - Percent-source labs (`agent-core`, `gcp-agent-platform-lab`): the `BOOTSTRAP` constant in their
    `tools/build_notebooks.py`. Change it there, then regenerate: `python3 tools/build_notebooks.py`.
  - All other notebooks: `python3 tools/inject_colab_bootstrap.py <lab-dir> …` (stdlib-only, idempotent —
    re-running replaces the cell, never duplicates it, never touches a lab's own cells).
  - After any change, regenerate: `python3 tools/gen_colab_index.py` (rewrites each layer README's Colab-links section, between `<!-- colab-links -->` markers, plus the `COLAB.md` setup guide).
- **Redo an exercise:** `git restore <notebook>` returns it to the committed blank; for percent-source
  labs, re-run `python3 tools/build_notebooks.py`. (This is why exercises are committed blank.)

## Reorg checklist (run when content arrives in `raw/`)
1. **Before-manifest:** file count + bytes under `raw/`.
2. Understand & classify each item (above).
3. **Propose the mapping to the user; confirm anything ambiguous.**
4. `mkdir -p` layer/topic folders; **`mv -n`** items in (whole folders, so nothing is half-moved).
5. Strip any per-item `.git` and build junk the drop brought in (it's a mono-repo).
6. Make notebooks Colab-ready (injector or build-script) and regenerate the per-layer Colab links (`tools/gen_colab_index.py`).
7. Update affected layer `README.md`s, this file's decisions log, root `README.md`.
8. `git add -A && git commit` and `git push`. Verify `raw/` holds only `.DS_Store` + `README.md`.

Handy:
```bash
cd ~/code/learning
python3 tools/inject_colab_bootstrap.py <layer>/<topic>/<lab>   # colab-ify hand-written notebooks
python3 tools/gen_colab_index.py                                # rebuild per-layer Colab links + COLAB.md
find . -maxdepth 5 -name '*.ipynb' ! -path '*/.ipynb_checkpoints/*' | wc -l
```

---

## Decisions log
- **2026-09-19 — initial 7-layer reorg.** 10 labs + a primer moved from `raw/` into layers 06–07;
  `agentic-scaling-lab`→06 (rate limits/admission/cost, x-ref 05); `gcp-agent-platform-lab`→07 (x-ref 06);
  four long-running-agent labs kept side by side under `07-.../long-running-durable/`;
  `vector_stores`→`07-.../retrieval-rag/`.
- **2026-09-19 — mono-repo + Colab conversion.** Removed 9 nested `.git` (no remotes; history discarded
  by choice) and the checked-in `.venv`, `__pycache__`, `*.pyc`, caches, egg-info, `.DS_Store`. Made
  the tree a single **private** repo `aniryou/full-stack-agentic-engineer` (a machine security control blocks public pushes; notebooks clone it on Colab via a `GH_TOKEN` secret). Made all **100** organized
  notebooks Colab-ready; normalized 3 in-progress exercise notebooks back to blanks first.
  `raw/` gitignored (except its README). **Tracked baseline: ~483 files, 100 notebooks.**
- **2026-09-19 — second raw sort.** Added **`00-foundations/`** (transformers, gpu-capacity-planning, model-landscape); populated **01-hardware** (gpu-primer, gpu-deployment) and **04-inference-engine** (flash-attention, paged-attention, kv-cache); added Mistral variants beside their counterparts in 06/07; added `embeddings-lab`, `rag-from-scratch`, `vector-databases-primer.md` to `07/retrieval-rag`. Renamed cryptic `files*/` drops to topic folders, consolidated the duplicated transformer lab, dropped byte-identical dupes, stripped per-item `.git`/junk, and made all new notebooks Colab-ready. **New baseline: ~165 notebooks.**

- **2026-09-20 — public-facing rewrite.** Prepared the repo to be opened up: the two `agent-fundamentals` labs became `agent-core` and `gcp-agent-platform-lab` (package `agentlab`), the long-running-agents drill file became `02_design_drills.md`, and every "what to say" callout now addresses a design review ("The one-minute version", "In one sentence", "In a design review", "Walking the design in a review", "Design drills"). Dropped the vendor-seat material from the Mistral primer exercises and the Mistral scaling primer (objection handling, partner motion, numbers drill, partner-addressed callouts). **Writing rule from here on:** content is addressed to an engineer explaining a design in a design review — never to a role or an employer. Vendor stacks (GCP, Mistral, AWS, …) are the same concepts worked per provider. Repo made **public** the same day; the Colab bootstrap cells and `COLAB.md` dropped the `GH_TOKEN` path (plain clone).

## Housekeeping
- `07-.../long-running-durable/00_primer.md` is byte-identical to the primer inside
  `long-running-agentic/`. Kept as the topic entry point; dedupe if desired.
- Root `.gitignore` keeps caches/venvs/`.DS_Store` out; per-lab `.gitignore` files are retained too.
