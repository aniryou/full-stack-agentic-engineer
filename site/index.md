---
template: home.html
title: Full-stack agentic engineer
description: Learn the LLM serving stack, from GPUs to agents, one topic at a time. Every topic goes from a primer to a from-scratch core to a complete lab, and runs free on a laptop.
hide:
  - navigation
  - toc
---

<!--
  The landing page is laid out by site/overrides/home.html.
  The text below is rendered inside its "Built with Claude" section, so it can be edited as Markdown.
-->

Everything in this repository was written by Claude: the primers, the code, the tests, the notebooks and the
reviews, under the owner's direction. The primer-core-lab topics in layers 01 to 05 were built by Claude agents
and then checked by adversarial Claude reviewers (concepts, runnability, pedagogy) before they were merged. In the owner's words:
*thanks to Claude we now have a teacher we never had before.*

The numbers follow one rule. A figure in a primer is computed by code in the repo and pinned by its tests,
or it is marked `(verify)` with a date. Simulator output is labelled **simulated**; bundled tool output is
labelled **sample output (illustrative)**.

Found a mistake? [Open an issue](https://github.com/aniryou/full-stack-agentic-engineer/issues) with the file,
the section and what you expected. A wrong number is a bug, and it gets fixed like one.
