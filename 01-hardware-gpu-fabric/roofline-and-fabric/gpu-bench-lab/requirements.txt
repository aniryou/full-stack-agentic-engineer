# The library needs numpy. The rest runs the notebooks and tests (tier T0: any CPU, no GPU).
numpy>=1.24
pytest>=7.4
nbformat>=5.9
nbclient>=0.9
ipykernel>=6.25
jupyterlab>=4.0
# Tiers T1/T2 (a CUDA GPU) also need PyTorch: preinstalled on Colab/Kaggle, else `pip install -e '.[gpu]'`.
