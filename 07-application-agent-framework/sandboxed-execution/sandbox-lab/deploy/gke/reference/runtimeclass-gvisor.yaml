# REFERENCE ONLY: GKE creates RuntimeClass 'gvisor' with the first GKE Sandbox node pool (handler and
# scheduling fields: verify with `kubectl get runtimeclass gvisor -o yaml`). apply.sh applies this only if
# the object is missing.
#
# Generated by tools/render_manifests.py from sandboxlab/k8s/render.py - edit there and re-run.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: gvisor
scheduling:
  nodeSelector:
    sandbox.gke.io/runtime: gvisor
  tolerations:
    - key: sandbox.gke.io/runtime
      operator: Equal
      value: gvisor
      effect: NoSchedule
