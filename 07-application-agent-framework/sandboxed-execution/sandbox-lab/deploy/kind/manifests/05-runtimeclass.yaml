# RuntimeClass sandbox-runc: handler runc (kind has no gVisor), pinned to the tainted sandbox worker through its scheduling field.
#
# Generated by tools/render_manifests.py from sandboxlab/k8s/policy.py - edit there and re-run.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: sandbox-runc
  labels:
    app.kubernetes.io/part-of: sandboxlab
handler: runc
scheduling:
  nodeSelector:
    sandboxlab/pool: sandbox
  tolerations:
    - key: sandboxlab/pool
      operator: Equal
      value: sandbox
      effect: NoSchedule
