"""k8s — sandboxes on Kubernetes: typed builders (``manifests``), policy as data rendered into
every enforcement point (``policy``), the admission chain predicted offline (``admission``),
pod-per-execution and warm-pool runners (``runner``), and the renderer (``render``)."""
