# The real models: all-MiniLM-L6-v2 (~90 MB) and cross-encoder/ms-marco-MiniLM-L-6-v2.
# sentence-transformers pulls in torch + transformers (a multi-GB install); CPU is fine.
# Colab already ships both, so there the notebooks use the real model with no extra install.
-r requirements.txt
sentence-transformers>=2.6
