# Fix plan for the 2026-09-26 adversarial review

Tracks how every finding of [`2026-09-26-adversarial-review.md`](2026-09-26-adversarial-review.md) (PR #13) is being
addressed. Work is cut into **packages** with disjoint file ownership so they can run in parallel; each package lives on
its own branch, is fixed and then independently verified by a second agent, lands as its own draft PR, and is squash-merged
to `main` as soon as it passes. This file is the ledger: update it whenever a package changes state.

Orchestration: one central session plans, launches and merges; the fixing and verifying agents run on Opus with a shared
workflow script (`fixpkg.js`: fix → verify → re-fix up to twice). Branch prefix: `claude/clever-hypatia-ma6t1l-`.
Tracking PR: the `claude/clever-hypatia-ma6t1l` branch (this file and the review document).

## Resuming after an interruption (usage cap, session loss)

1. `git fetch origin && git branch -r | grep clever-hypatia` — every package branch that exists was pushed by its fixer.
2. Open PRs: `gh pr list` (or the GitHub UI) filtered on the branch prefix; a package with an open PR is fixed or under
   verification; a merged PR means done — mark it below.
3. For a package without a PR: its fixer did not finish. Re-launch it with the package spec below (scope + findings +
   verify) on the existing branch if one exists (keep its commits), otherwise from `origin/main`.
4. Waves are ordered by dependency (below). Never start wave 2 (notebook edits) before `w1-build` has merged: it
   regenerates every percent-source notebook once (stable cell ids), and notebook edits before that would conflict.
5. After each wave: regenerate the Colab index (`python3 tools/gen_colab_index.py`) and the site nav
   (`python3 tools/site/build_site_content.py`), `mkdocs build --strict`, `python3 tools/orchestration/mdlinks.py` on the
   changed `.md` files, and refresh this ledger, `STATUS.md` and the `CLAUDE.md` decisions log.

## File ownership rules (wave 1)

Owned by nobody in wave 1 (so no package edits them): `CLAUDE.md`, `tools/orchestration/STATUS.md`, the eight layer
`README.md` files, every `.ipynb`, every notebook source (`notebooks_src/*.py`), `tools/gen_colab_index.py` runs.
`mkdocs.yml` is only ever regenerated by `tools/site/build_site_content.py`, never hand-edited.

## Packages

| Package | Branch suffix | Scope | Review items | State |
|---|---|---|---|---|
| `w1-root` | `-w1-root` | `README.md`, `CURRICULUM.md`, `COMPUTE.md`, `LICENSE`, `site/guide/{about,how-to-use}.md`, `site/index.md` (price line), `raw/README.md` | #1 (§2 gap column), #5 (LICENSE), #12 counts in root docs; §3 05.3 + drill 5, RTX PRO 6000; §4 CLAUDE.md-as-map, raw/README; §6 root README + CURRICULUM items; §8 T2 price; §10 rows | **MERGED** (PR #15, 2d8ee6b; 2 rounds; root MIT LICENSE, curriculum ≈ 267 h) |
| `w1-build` | `-w1-build` | every `build_notebooks.py`, every `run_notebooks.py`, the notebooks they regenerate, lab Makefiles | #7 (stable ids, bootstrap in 3 builders, honest `--expect-fail`); §5 rebuild hazards | **MERGED** (PR #18, 2491d10; 3 rounds + a one-line orchestrator fix: `--expect-fail` now flags every ellipsis-error spelling; 130 notebooks changed id-only, 0 content diffs; 24 labs gain `tests/test_notebook_tooling.py`) |
| `w1-l0001` | `-w1-l0001` | `00-foundations/**`, `01-hardware-gpu-fabric/**` minus notebooks/builders/layer READMEs | §3 18× ratio (3 files), Small 4, capacity.py units, prefill FLOPs, B200 memory, gpubench noise; §4 vendor framing in 00; §10 counts; §1 roofline-core requirements; transformers README | **VERIFIED** (PR #20, 2 rounds) — held: `capacity.py` now uses GB = 1e9 throughout, which breaks `vllm-serving-lab/tests/test_sizing.py` (×1024 → ×1000, `spare / GiB` → `spare / 1e9`) and rl-core notebook 05's pinned numbers (4.77 → 5.12 GPUs, 0.26 → 0.28, 2.57 → 2.75, 0.14 → 0.15, 5 → 6, 139 → 154, 16.7 → 18.5 ms, 187/209 → 174/195, KiB → kB). Fixup lands on the same branch after `w1-build` merges (needs the stable-id rebuild), then #20 merges |
| `w1-l020305` | `-w1-l020305` | `02-cuda-nccl-runtime/**`, `03-kubernetes-gpu/**`, `05-orchestrator/**` minus notebooks/builders/layer READMEs | §3 02 §7.4/7.5, occupancy CC 10.3/12.0, DynamicResources, pipeline/device-plugin diagram, allocation policies, KEDA sentence, RAG share; §1 `k8sgpu pending`, `bench --quick`; §10 timings | **MERGED** (PR #17, 2fefd6a; verified in 1 round; CC 10.3/12.0 rows carry `(verify)`) |
| `w1-l04` | `-w1-l04` | `04-inference-engine/**` minus notebooks/builders/layer README | §3 vLLM flags, FA figures, kernel-primer verify lists, kv-cache ops/byte, `NUM_BLOCKS`, decode wording, knob table, seed, total-vs-free, perf.py cross-cite; §4 "home cluster"; §10 counts, source-map pace; §1 kv-cache torch note | **MERGED** (PR #19, 6f56e4f; verified in 1 round; H6 partial: the kv-cache notebooks still need outputs or a numpy port → `w4-kernels-t0`) |
| `w1-l06` | `-w1-l06` | `06-gateway/**` minus notebooks/layer README (+ regenerated `mkdocs.yml`) | §3 DPoP `token_type`, aud/actor_token/may_act, serving.py cross-cite; §4 `docs/client/` binaries + second primer, vendor/MOU framing; §1 flaky test; §7 `scalelab` name clash (documented); §10 counts | **MERGED** (PR #22, 4d2def9; 3 rounds; plus the six identity-core notebooks switched to `actor_token=agent.credential`, executed clean) |
| `w1-l07a` | `-w1-l07a` | `07-…/agent-fundamentals/**` minus notebooks/builders | §3 `@tool` schema/errors, gen_ai.* attributes, MCP revision (verify); §6 the primer that is not there (30+ citations); §4 interview residue, "separate repository", `git clone <this repo>`; §1 KeyError stop | **MERGED** (PR #16, eca12fd; verified in 1 round; also fixed the MCP error code −32022) |
| `w1-l07b` | `-w1-l07b` | `07-…/long-running-durable/**`, `07-…/retrieval-rag/**` minus notebooks/builders (+ regenerated `mkdocs.yml`) | §3 lra-core lease bug, HNSW `M0`; §1 Mistral durable Python ≥ 3.12, lra-gcp quick start, rag-from-scratch T0 embedder, vector_stores README/nltk; §2 embeddings-lab checks; §7 byte-identical duplicates removed | **MERGED** (PR #21, 1b9fd91; 2 rounds; four byte-identical duplicates removed, `mkdocs.yml` regenerated) |
| `w1-site` | `-w1-site` | `mkdocs.yml`, `tools/site/**`, `site/{javascripts,stylesheets,overrides,assets}`, `pages.yml`, `requirements-site.txt`, `tools/gen_colab_index.py` (code) | #10 (MathJax prices, sidebar, nav titles, plumbing pages, weight, search, anchors, small items); #11 `_worked` misclassification in both generators | **MERGED** (PR #23, c9f5f9d; 3 rounds; site 456 MB → 77 MB, search index 11.5 → 6.0 MB, 0 published `.ipynb`, 0 dead notebook anchors, MathJax on 5 pages only) |
| `w2-nb-*` | `-w2-nb-<layer>` | notebook sources and `.ipynb` per layer | #3 (the 15 checks that pass wrong/empty answers), §1 ellipsis/KeyError first-exercise errors, §4 interview residue in notebooks, §8 TOC anchors (06 identity), notebook text changes reported by wave 1 | **RUNNING** (launched 16:50Z from `main` at 2491d10 as `w2-nb-020305`, `w2-nb-04`, `w2-nb-06`, `w2-nb-07`; the 00/01 notebook items ride the `w1-l0001` fixup) |
| `w3-readmes` | `-w3-readmes` | the eight layer READMEs, two new 06 topic READMEs, root README rows | #8 (README style for 00/06/07, tier tables, drop the CLAUDE.md sorting metadata, "Where this layer sits" consistency, 03/05 "Covers" overclaims), #11 one name per layer, wave-1 count corrections, Colab index regeneration | **RUNNING** (launched 16:25Z from `main` at c9f5f9d; touches only READMEs, root docs, the two generators — no notebooks — so it runs alongside the build package; run id wf_90da4dc0-bd7) |
| `w3-ci` | `-w3-ci` | `.github/workflows/tests.yml`, delete 3 nested `ci.yml` | #6 (tests + rebuild `git diff --exit-code` + mdlinks + site generator check) | wave 3 — after `w1-build` merges |
| `w3-naming` | `-w3-naming` | notebook directory conventions → `notebooks/` + `solutions/` everywhere; generators, run scripts, Makefiles, READMEs | #11 five notebook conventions | wave 3 — after wave 2; renames only |
| `w4-kernels-t0` | `-w4-kernels-t0` | `04-inference-engine/{kv-cache,paged-attention,flash-attention}` | "Weeks": a numpy core so module 04.0 is truly T0 (§1 kv-cache torch) | wave 4 |
| orchestrator | `claude/clever-hypatia-ma6t1l` | `CLAUDE.md` decisions log + percent-source list, `tools/orchestration/STATUS.md`, sandbox-path scrub in `tools/orchestration/`, this ledger | §4 process residue, #5 | continuous |

## Decisions left to the owner (not executed without a yes)

- **Consolidate the 07 durable sub-tree** to one core + one lab (review §7 proposes keeping `lra-core` → `lra-gcp`, folding
  the unique pieces of `long-running-agents-core/-gcp` in, keeping the Mistral lab only if it installs). Wave 1 removes only
  byte-identical duplicates.
- **Fold the three Mistral variants into adapters** (one lab with a `mistral` extra; `scalelab` backend switch; the identity
  core importing the five moves). Wave 1 keeps the copies in sync and documents the `scalelab` name clash.
- **Two new topics** the review says the title implies: an LLM gateway (06) and agent memory (07). Both are in the
  curriculum backlog; they are builds, not fixes.
- **Licence**: wave 1 adds a root MIT `LICENSE` (the wording of the 15 MIT lab licences) covering the primers, curriculum and
  compute guide; change it if you want a different licence for the prose.

## Workflow run ids (this session)

`w1-root` wf_fc750b9b-5c7 · `w1-build` wf_6f9f47b5-377 · `w1-l0001` wf_943e3b2d-db1 · `w1-l020305` wf_e1091a23-3b5 ·
`w1-l04` wf_b4bed3fa-216 · `w1-l06` wf_06176f63-07a · `w1-l07a` wf_2e5bb0b3-499 · `w1-l07b` wf_7c86f40b-fce ·
`w1-site` wf_e2eca8bb-a9d · `w3-readmes` wf_90da4dc0-bd7 · `w1-l0001` fixup wf_d992ec71-98a · `w2-nb-020305` wf_04738cec-a92 · `w2-nb-04` wf_1621f7d9-ce1 · `w2-nb-06` wf_e53c5b2d-936 · `w2-nb-07` wf_456bdda7-f53. Script: `fixpkg.js` beside this file; each run's journal is under the session's `subagents/workflows/<run id>/journal.jsonl`.

## Log

- 2026-09-26 14:40Z — plan written; nine wave-1 packages launched in parallel from `main` at `2c9f667`.
- 2026-09-26 15:12Z — `w1-l07a` merged (PR #16). Its notes for wave 2 (notebook-source edits in agent-core, mistral-agent-core and the gcp lab: 23 dead primer citations, interview wording, the `45 minutesutes` typo, notebook 14's legacy gen_ai attribute names, notebook 05/09 text) are queued for `w2-nb-07`.
- 2026-09-26 15:20Z — `w1-l020305` merged (PR #17). Wave-2 note: orchestrator-core notebook 03 source line 253 ("about 10 % of the requests" → "about 15 %… three quarters of the uncached prefill"). Wave-3 note: layer READMEs 02/03/05 and CURRICULUM/COMPUTE test counts (02: 133 / 123+2 skipped, 15–30 s; 03: 51 core, a few seconds, 120 lab; 05: 58 core).
- 2026-09-26 15:25Z — `w1-l04` merged (PR #19). Wave-2 notes: kv-cache notebooks print KiB as KB and mix 128,000 tokens with decimal GB (cells 2c75196e / c4a3933f); flash-attention practice cell 14 'run it on the cluster' → a free T4 / `TRITON_INTERPRET=1`. Wave-3 notes: 04 README rows (kv-cache tier T0 + torch; flash-attention 'a few seconds'; mini-engine '~5 s'; vllm-internals 'four ~2 h sittings, about 8.5 h'); CURRICULUM 04.8 +2.5 h.
- 2026-09-26 15:35Z — `w1-root` verified (PR #15, 2 rounds); held until `w1-l07b` merges. Integration notes: `site/index.md` front-matter description and `site/overrides/home.html:32` still say every topic has primer + core + lab (fix in `w3-readmes`, which owns naming across surfaces); CURRICULUM §3.4 names 'the lra primer' — after `w1-l07b` deletes `lra/primer.md`, link `lra-gcp/docs/primer.md`; consider a NOTICE file for the Apache-2.0 upstream CRD snapshots inside MIT labs; CLAUDE.md log entry: curriculum ≈ 267 h (07.4 = 28 h), root MIT LICENSE, nine-topic wording.
- 2026-09-26 15:45Z — `w1-l0001` verified (PR #20, 2 rounds); held for a cross-package fixup (see its row). Also stale but not failing, for wave 2: the 00 capacity practice notebooks still divide by 1024 and print 'KB' (bank example 95.3/381.3 → 88.8/355.1 sessions per GPU); thinking-lab notebook 04 prose (5 GPUs, 2.57 vs 0.14, 229 GB → 6, 2.75 vs 0.15, 246 GB); `tools/orchestration/facts/rl-and-thinking-models.md` L301–308. Wave 3: 01 README (roofline-core notebooks need `requirements-notebooks.txt`), CURRICULUM gpu-bench-lab '89 tests' → 92 (90 pass, 2 skip without safetensors).
- 2026-09-26 15:55Z — `w1-l07b` merged (PR #21), then `w1-root` (PR #15). Wave-2 notes from l07b: embeddings-lab `python build.py ex04 ex05` + injector (the committed ex04/ex05 notebooks still carry the weak checks); long-running-agents-mistral notebooks 01 cell 19 / 02 cell 14 need a 'Python 3.12–3.14' note; lra-core solution notebook 01 stored outputs lack the new `updated` field. Wave 3: 07 README line ~51 (one copy of the durable primer). Orchestrator: CLAUDE.md Housekeeping bullet on the byte-identical primer is stale; COMPUTE.md:367 (rag-from-scratch model download) — check after #15.
- 2026-09-26 16:05Z — `w1-l06` merged (PR #22). Residuals noted by its verifier, not fixed: the GCP identity lab skips `may_act` when no actor token is given; the cores accept a delegated token for APP_AUDIENCE as a subject without chaining `act.act`; the two `scalelab/clock.py` files now differ (reconcile with the Mistral-fold decision); `scalelab.serving` GPUS['h100'] fp8 figure is the sparse 1979 TFLOP/s; gcp-lab `docs/primer.md` §11.1 'Expect:' answers read like a rubric (writing rule, wave 3).
- 2026-09-26 16:20Z — `w1-site` merged (PR #23) after a `main` merge with the nav regenerated. Integration notes: run `tools/gen_colab_index.py` now (new `*_worked` rule: long-running-agents-gcp's worked notebooks are lessons, lra-gcp `worked/` stays answer keys); lra-gcp README line 8 vs 38 contradict on what `worked/` is (wave 3); the sidebar fix is CSS keyed to Material 9.7.x markup; the generator only prints 'markers missing' if `mkdocs.yml` loses its markers — never take `main`'s copy in a conflict, regenerate. S8 (prev/next along the curriculum, module chip) has a design in the w1-site notes; not implemented.
- 2026-09-26 16:25Z — check-in: Pages green on every merge (site run 12 included); `w1-build` in its third fix round (its verifier insists the rebuild be a pure no-op); `w3-readmes` launched early since it cannot conflict with notebook work. Next check-in 17:21Z.
- 2026-09-26 16:50Z — `w1-build` merged (PR #18). Notes: percent-source labs are now the 21 BOOTSTRAP-constant labs plus 00/transformers (`practice/build_notebooks.py`), long-running-agents-gcp and lra-gcp, whose builders emit the injector's cell themselves (CLAUDE.md list to update); every lab's test count grows by 8 (7 run + 1 skip, ~10 s) from the tooling tests — recount READMEs at the end; sandbox-lab notebook 05 reports FAIL(env) as root under a 0700 /tmp (confirm on Colab); rag-from-scratch `tools_build_notebooks.py` emits no Colab cell and its 00_setup notebooks are stale (→ `w2-nb-07`); 00/transformers now has tests/ but no pytest in requirements. Launched the `w1-l0001` fixup on its branch and four wave-2 notebook packages.
- 2026-09-26 17:10Z — `w2-nb-06` merged (PR #25, 12c9655; 1 round). Not reproduced: the 06 identity notebooks have no hand-written TOC cells — the 179 dead anchors were mkdocs-jupyter TOC entries, already fixed by the site hook (#23). Writing-rule residue for a final sweep: `agentic-scaling-lab/notebooks/01_scaling_math.ipynb` cell 1 ('The system-design round grades envelope arithmetic'), the Mistral scaling notebook 01 takeaways ('Verify before the conversation', 'the customer's GPUs').
- 2026-09-26 17:20Z — `w2-nb-07` merged (PR #26, e50e20e; 1 round): agent-core/mistral checks fail the review's wrong answers, gcp-lab cites in-repo primers and writes the current OTel GenAI names, MCP results carry `resultType: complete`, rag checks pin exact RRF and all-gold recall, rag builder emits the Colab cell, Mistral workflow cells guard Python 3.12–3.14. Follow-ups: gcp lab README says 147 unit tests (now 156 + 1 skip) — counts recounted in the final pass; `embeddings-lab/build.py` (jupytext) mints random cell ids → make it stable in `w3-ci`; rag-from-scratch README could name hit@k / recall@k / MRR.
- 2026-09-26 17:30Z — `w2-nb-04` merged (PR #28, 954f3d2; 1 round): paged/flash practice checks fail the review's wrong answers and stop cleanly; vllm-internals notebook 01 never prints ✅ for an empty answer (`USE_REFERENCE = True` reads on with the reference — any validator re-running it for the primer numbers must set it); kv-cache notebooks print KiB/GiB with GB in brackets, 01 committed with CPU outputs (`IDENTICAL: True`). Loose ends for the final pass: `kv-cache/kv-cache-primer.md` line 5 ('the notebooks print decimal GB' → 'binary units too, with decimal GB in brackets'); 04 README kv-cache tier 'T0 + torch (CPU)' (in `w3-readmes`); vllm-internals README could mention the stop/USE_REFERENCE behaviour. Environment: download.pytorch.org is blocked by the egress proxy, so torch came from PyPI with CUDA deps (~4 GB, deleted after the merge).
