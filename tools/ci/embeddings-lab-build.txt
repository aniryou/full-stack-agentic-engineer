# Exact versions for 07-application-agent-framework/retrieval-rag/embeddings-lab/build.py.
# That build executes the notebooks and commits their outputs (plots included), so the
# notebooks CI job rebuilds them with these pins and requires `git diff --exit-code` to pass.
# Bump them together with a rebuild of the lab's notebooks.
numpy==2.4.6
matplotlib==3.11.2
matplotlib-inline==0.2.2
pillow==12.3.0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.66.0
kiwisolver==1.5.1
pyparsing==3.3.3
jupytext==1.19.5
nbclient==0.11.0
nbformat==5.11.1
ipykernel==7.3.0
ipython==9.17.1
jupyter_client==8.10.0
