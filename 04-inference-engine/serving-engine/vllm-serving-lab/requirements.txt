# The library (T0): an async HTTP client/server and the Prometheus client.
aiohttp>=3.9
prometheus_client>=0.20
# Notebooks and tests.
pytest>=7.4
nbformat>=5.9
nbclient>=0.9
ipykernel>=6.25
jupyterlab>=4.0
numpy>=1.24
pyyaml>=6.0
# T1 only (GPU box, not needed for T0):  pip install "vllm>=0.30"
