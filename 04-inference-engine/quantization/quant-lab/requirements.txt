# The library (T0): numpy only.
numpy>=1.24
# Notebooks and tests.
pytest>=7.4
nbformat>=5.9
nbclient>=0.9
ipykernel>=6.25
jupyterlab>=4.0
pyyaml>=6.0
# Optional cross-checks (tests skip them when absent): safetensors, kubernetes-validate, torch (CPU is fine).
# T1 only, each in its own environment:  pip install "llmcompressor==0.14.0"  |  "vllm==0.30.0"  |  "lm_eval[vllm]==0.4.13"
